services:
  taskboard:
    container_name: pyjobkit-taskboard
    build:
      context: .
      dockerfile: examples/taskboard/Dockerfile
    ports:
      - "8000:8000"
    restart: unless-stopped
    environment:
      - DOCKER_BUILDKIT=0
